// implement the formatString function in order to inject the values
// received in the tokens array into the string parameter
// the replacement should be done based on the position of the elements
const formatString = (string, ...tokens) => {

};

console.log(formatString('this is a {0} string and we\'ve {1} it', 'nice', 'modified'));
// 'this is a nice string and we've modified it'
